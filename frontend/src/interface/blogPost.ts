export interface PostInterface {
  slug: string
  title: string,
  tags: string,
  date: string,
  author: string,
  isPublish: boolean,
  content: string
}

export interface VanishedInterface {
  slug: string
  title: string,
  tags: string,
  date: string,
  author: string,
  content: string
}